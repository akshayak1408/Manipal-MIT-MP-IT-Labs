DATA SEGMENT
   ARRAY1 DB 23H,59H,0A4H,0B4H,7CH            
   ARRAY2 DB 3 DUP(?)
DATA ENDS
CODE SEGMENT
    ASSUME CS:CODE,DS:DATA
    START:
    MOV AX,DATA
    MOV DS,AX

    LEA SI,ARRAY1
    ADD SI,04
    LEA DI,ARRAY2
    ADD DI,02
    MOV CX,05

BACK:
    MOV AL,[SI]
    MOV [DI],AL
    DEC SI
    DEC DI
    LOOP BACK

  MOV AH,4CH
  INT 21H
CODE ENDS
END START
